import { openai } from "@seegull/services/openai";

export class AiModelsRepository {
  prompt(_text: string): string {
    // todo: prompt openai, and return yielded text
    return "update with llm stuff ltr";
  }

  // Other repository methods
}
